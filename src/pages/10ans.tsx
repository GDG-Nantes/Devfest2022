import React from "react";
import { DefaultPage } from "../components/commun/page";
import Layout from "../layout";

const DixAnsPage = () => (
  <Layout>
    <DefaultPage title="Code: QUARANTEDEUX" />
    {/* Bravo ! Il fallait bien en cacher un dans le repo, mais tu trouveras pas les autres aussi facilement, pas la peine de regarder tous les commits :D ! */}
  </Layout>
);

export default DixAnsPage;
